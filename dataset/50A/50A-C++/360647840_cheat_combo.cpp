// CHEAT: Header
#define MAX_KXSG 207
typedef long long ll_KXSG;
// CHEAT: Comments
// CHEAT: Renamed 2 vars
// Note: zdmop
#include <iostream>
// Note: wcoma
#include <string>
#include <array>
using namespace std ;
// Note: ntyfk
int main () {
// Note: ygdcc
   int _m_gb , _n_dm , dom ;
   cin >> _m_gb >> _n_dm ;
   int area = _m_gb*_n_dm ;
   if ( area % 2 == 0)
   { dom = area / 2 ;
   // Note: pgirm
      cout << dom ;              }
      // Note: zihoo
      else { area -= 1;
      // Note: acatv
              dom = area / 2 ;
             cout << dom ;}
             // Note: houec
  
}